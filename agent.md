# Bescuit Operation Assistant 页面使用说明

本文档面向实际使用者，说明酒吧经营助手中每个页面的入口、用途和主要操作项。

## 全局导航

应用左侧为主导航栏，包含以下入口：

| 导航项 | 路径 | 用途 |
| --- | --- | --- |
| 首页 | `/` | 查看今日状态，并快速进入常用工作 |
| 营业额录入 | `/sales/new` | 录入或修改某天各收款渠道的营业额 |
| 支出录入 | `/expenses/new` | 录入某天供应商支出，支持选择历史供应商或输入新供应商 |
| 发票核对 | `/invoices/new` | 上传发票文件，创建发票 intake 任务 |
| 数据分析 | `/analytics/monthly` | 查看月度收入、支出、利润和结构分析 |
| 日历概览 | `/analytics/calendar` | 按日历查看每天的收入和支出摘要 |

## 首页

路径：`/`

首页用于快速判断今天是否还有待处理工作，并进入最常用流程。

### 页面信息

- 显示当前日期。
- 显示今日营业额是否已录入。
- 显示最近操作时间。
- 显示本月已录入发票数量。
- 显示待核对发票数量。

### 可操作项

- 点击“输入今日营业额”，进入营业额录入页面。
- 点击“输入一张发票”，进入发票 intake 页面。
- 点击“查看本月分析”，进入月度数据分析页面。
- 点击“日历概览”，进入日历概览页面。

## 营业额录入

路径：`/sales/new`

营业额录入页面用于登记某一天的实际收款金额。页面支持按日期读取已有记录，也支持保存草稿或正式提交。

### 页面信息

- 日期选择器：用于选择要录入或查看的营业日期。
- 收款渠道：按渠道录入金额，目前包含 BBVA、CAIXA、EFECTIVO 等渠道。
- 总计：自动汇总所有渠道金额。
- 备注：可填写当天营业额相关补充说明。
- 状态提示：保存草稿、提交成功或加载已有记录后会显示反馈信息。

### 可操作项

- 修改日期，查看或录入对应日期的营业额。
- 在各收款渠道中输入金额。
- 填写可选备注。
- 点击“保存草稿”，保存未最终确认的数据。
- 点击“确认提交”，提交当天营业额。
- 点击“返回首页”，回到首页。

### 使用建议

- 如果当天金额还未完全确认，先保存草稿。
- 金额确认无误后再点击“确认提交”。
- 切换日期时，页面会尝试加载该日期已有草稿或已提交记录。

## 支出录入

路径：`/expenses/new`

支出录入页面用于登记某一天的供应商支出。页面支持选择日期、从历史发票供应商中选择供应商，也支持直接输入新的供应商名称。

### 页面信息

- 日期：支出发生日期。
- 供应商：来自 `invoices.supplier_name` 的历史供应商，或用户手动输入的新供应商。
- 价格：本次支出金额。
- 备注：可选补充说明。
- 当日已录入：提交后会显示所选日期最近录入的供应商支出，便于核对。

### 可操作项

- 修改日期，查看或录入对应日期的供应商支出。
- 从历史供应商中选择供应商，或直接输入新供应商。
- 输入本次支出金额。
- 填写可选备注。
- 点击“确认提交”，提交本次支出。

提交后系统会写入 `ledger_entries`，并作为 `manual` 来源的支出参与月度分析和日历概览。

## 发票 Intake

路径：`/invoices/new`

发票 intake 页面用于上传发票文件，并创建后续核对任务。页面会根据当前运行环境显示不同链路：

- Phase 6 异步链路：上传文件到 R2，登记 source document，创建 intake job，并投递到 Queue。
- Phase 5 本地版：在本地 fallback 流程中模拟创建 intake job。

### 页面信息

- 当前链路状态：显示“Phase 6 异步链路”或“Phase 5 本地版”。
- 上传区域：支持选择 PDF 或常见图片格式。
- 文件校验结果：展示文件大小、类型、是否可创建任务。
- 待创建任务列表：展示本次选择的文件和创建状态。
- 最近任务：展示最近的发票 intake job 及状态。

### 可操作项

- 点击文件选择框，上传 PDF 或图片。
- 在手机端点击“手机拍照”，直接拍摄发票图片。
- 一次选择多个文件，批量创建 intake 任务。
- 点击“创建 intake 任务”，为有效文件创建任务。
- 创建成功后，点击单个文件旁的“打开”，进入对应发票 review 工作台。
- 点击“打开最近任务”，进入最近一个发票核对任务。
- 在“最近任务”列表中点击任意任务，打开对应 review 页面。
- 点击“返回首页”，回到首页。

### 文件要求

- 支持 PDF 和常见图片格式。
- 单文件大小不能超过页面提示的上传限制。
- 未通过校验的文件不会创建 intake 任务。

## 发票 Review 工作台

路径：`/invoices/review/$jobId`

发票 review 工作台用于核对 AI 或系统抽取出的发票信息，并在满足条件后确认入账。该页面通常从发票 intake 页面进入。

### 页面信息

- 任务状态：显示当前 job 状态和 jobId。
- 文件信息：显示文件名和上传时间。
- 文档预览区：展示发票 PDF 或图片预览。
- 抽取信息：显示 provider、model、置信度和抽取警告。
- 发票抬头表单：核对供应商、发票号、日期、总金额、税额和备注等字段。
- 明细表格：核对商品、数量、单价，并映射到原料库。
- 入账阻塞项：显示缺少字段、金额格式错误或未匹配原料等问题。

### 可操作项

- 在文档预览区放大、缩小、旋转文档。
- 多页文档可切换上一页或下一页。
- 修改发票抬头字段。
- 修改明细行中的数量、单价和原料映射。
- 点击“保存草稿”，保存当前核对进度。
- 当所有入账条件满足后，点击“确认入账”。
- 点击“返回 intake”，回到发票 intake 页面。

### 状态说明

- Queue 排队中：任务已创建，正在等待异步处理。
- 异步抽取中：系统正在进行 OCR 或结构化抽取。
- 抽取失败：需要查看错误信息并重新处理或人工介入。
- 有入账阻塞项：仍需补齐必填字段、修正金额格式或完成原料映射。
- 已满足入账条件：可以确认入账。

## 数据分析

路径：`/analytics/monthly`

数据分析页面用于查看所选月份的收入、支出和利润情况。

### 页面信息

- 月份选择器：切换要查看的月份。
- 总收入：展示所选月份收入总额及较上月趋势。
- 总支出：展示所选月份支出总额及较上月趋势。
- 净利润：展示收入减支出后的结果及趋势。
- 利润率：展示利润率及较上月变化。
- 收入结构：按 BBVA、CAIXA、EFECTIVO 等渠道展示收入占比。
- 支出结构：按支出类别展示金额和占比。
- 周收支趋势：按周对比收入和支出变化。

### 可操作项

- 使用月份选择器切换不同月份。
- 查看收入结构图，判断主要收入来源。
- 查看支出结构，识别主要成本类别。
- 查看周收支趋势，判断月内经营波动。
- 点击“返回首页”，回到首页。

## 日历概览

路径：`/analytics/calendar`

日历概览页面用于按自然日查看所选月份每天的收入和支出摘要。

### 页面信息

- 本月总收入。
- 本月总支出。
- 本月净利润。
- 月历视图：每一天显示该日收入和支出摘要。
- 今日高亮：当前日期会在日历中突出显示。

### 可操作项

- 点击左箭头，查看上一个月。
- 点击右箭头，查看下一个月。
- 点击“今天”，回到当前月份。
- 在日历中查看每天是否有收入或支出记录。
- 点击“返回首页”，回到首页。

## 推荐日常使用流程

1. 打开首页，查看今日营业额、待核对发票和最近操作状态。
2. 当天结束营业后，进入“营业额录入”，填写各收款渠道金额。
3. 收到供应商发票后，进入“发票核对”，上传 PDF 或图片。
4. 从 intake 任务进入“发票 review 工作台”，核对发票抬头和明细。
5. 确认营业额和发票数据后，进入“数据分析”查看月度经营表现。
6. 需要按天追踪波动时，进入“日历概览”查看每日收支。

## 数据与环境说明

- 本地开发时，如果 Cloudflare bindings 不完整，系统会使用 fallback 数据流程。
- 生产或完整 Cloudflare 环境下，发票 intake 会使用 R2、D1、Queue 和 AI 抽取链路。
- 首页、分析页和日历页的数据会随营业额提交、发票保存或确认入账后刷新。
